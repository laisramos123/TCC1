server:
  port: 8082
  ssl:
    enabled: true
    key-store: classpath:certificates/resource-server-keystore.p12
    key-store-password: changeit
    key-store-type: PKCS12
    key-alias: resource-server

    trust-store: classpath:certificates/truststore.p12
    trust-store-password: changeit
    trust-store-type: PKCS12

    client-auth: want
    enabled-protocols: TLSv1.2,TLSv1.3

spring:
  application:
    name: resource-server

  datasource:
    url: jdbc:h2:file:./data/resourcedb;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  h2:
    console:
      enabled: true
      path: /resource-h2-console
      settings:
        web-allow-others: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
      data-locations: classpath:data.sql

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://localhost:8080
          jwk-set-uri: https://localhost:8080/.well-known/jwks.json

open-banking:
  resource-server:
    base-url: https://localhost:8082

  consent:
    validation:
      enabled: true
      auth-server-url: https://localhost:8080

open-finance:
  mtls:
    enabled: true
    client-certificate-path: classpath:certificates/resource-server-keystore.p12
    client-certificate-password: changeit

logging:
  level:
    root: INFO
    com.example.resource_server: DEBUG
    org.springframework.security: INFO
    org.springframework.web: DEBUG
    javax.net.ssl: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://postgres:5432/resourcedb}
    username: ${DATABASE_USERNAME:tcc_user}
    password: ${DATABASE_PASSWORD:tcc_password}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    defer-datasource-initialization: false

  h2:
    console:
      enabled: false

  sql:
    init:
      mode: never

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080
          jwk-set-uri: http://localhost:8080/.well-known/jwks.json

open-banking:
  resource-server:
    base-url: http://resource-server:8082

  consent:
    validation:
      auth-server-url: http://localhost:8080

server:
  ssl:
    enabled: false

---
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:testresourcedb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  h2:
    console:
      enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080

server:
  ssl:
    enabled: false
